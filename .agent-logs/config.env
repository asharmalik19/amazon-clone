# Read by .claude/hooks/capture.py to fill in log frontmatter.
# The header block of each log file is regenerated on every write, so changing
# a value here retroactively fixes the frontmatter of existing logs without
# touching any captured entry.
AUTHOR=asharmalik19
PROJECT=8x-assignment
TOOL=claude-code
